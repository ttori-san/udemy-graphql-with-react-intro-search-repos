const GITHUB_TOKEN = process.env.REACT_APP_GITHUB_TOKEN;//ok
// console.log({GITHUB_TOKEN});//({}) === key:value形式で出力
function App() {
  return (
    <>
      <div>Hello</div>
    </>
  );
}

export default App;
